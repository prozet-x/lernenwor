http {
    server {
      listen 80;

      location / {
        proxy_pass http://frontend:3000;
      }

      location /api {
        proxy_pass http://lernenwor:8080;
      }
    }
}

events {
    worker_connections 1024;
}